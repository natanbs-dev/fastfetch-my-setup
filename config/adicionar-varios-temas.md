# ADICIONAR VARIOS TEMAS 

## METODO

- Copie o arquivo para a pasta de share padrão do fastfetch e, através do `fastfetch -c examples/[nome].jsonc` , executa p arquivo da pasta de presets/examples para poder consultar assim que for necessário com o comando abaixo: 

:`cp -r omarchy.jsonc /usr/share/fastfetch/presets/examples/` 
